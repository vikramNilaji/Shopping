#Dependency directories (never commit these,they are massive)
node_modules/

#Environment variables / Secrets(crucial for security)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

#Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

#System files (OS junk)
.DS_Store
Thumbs.db

#Build/Frontend output (we will need this when we add React)
dist/
build/